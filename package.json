{
  "name": "osp-billiards-play-hostinger",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "mysql2": "^3.14.3"
  },
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "start": "node --env-file=.env.local server/index.mjs",
    "setup": "node server/setup.mjs",
    "check": "node --check server/index.mjs && node --check server/setup.mjs && node --check server/database.mjs"
  }
}
